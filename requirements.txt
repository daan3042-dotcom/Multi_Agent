# Kernafhankelijkheden voor het fundament (sectie A). sqlite3 en dataclasses
# zijn standaardbibliotheek. anthropic is nodig voor qc.qc.default_llm_review()
# (A.5) -- de daadwerkelijke API-call wordt pas gedaan door de client die de
# aanroeper meegeeft (dependency injection), dus deze dependency is alleen
# nodig als je de default-implementatie ECHT aanroept, niet om de tests te
# draaien (die gebruiken een fake client).
anthropic

# Sectie B: domain agents halen hun databron zelf op (FRED, Alpha Vantage FX)
# -- zelfde conventie als analyst_agent.ai's data/-modules.
requests

# Alleen voor tests
pytest
